# راهنمای Rebuild و دیپلوی فرانت‌اند

## تغییراتی که انجام شد:

✅ فایل `src/services/api.js` به‌روز شد
- URL پیش‌فرض از `http://localhost:8081` به `https://mcl-backend.liara.run` تغییر کرد

## مراحل Rebuild و دیپلوی:

### ۱. Rebuild فرانت‌اند
```bash
cd front
npm run build
```

این دستور یک پوشه `build` جدید می‌سازد که فایل‌های بهینه‌شده برای production را دارد.

### ۲. دیپلوی در لیارا
```bash
liara deploy --port 3000 --platform node --detach
```

یا اگر می‌خواهید فقط فایل‌های build شده را دیپلوی کنید:

```bash
liara deploy --port 3000 --platform static --detach
```

### ۳. بررسی تغییرات

بعد از دیپلوی:
1. برنامه را در مرورگر باز کنید: https://mcl-front.liara.run
2. Developer Console (F12) را باز کنید
3. به تب **Network** بروید
4. سعی کنید عملیاتی انجام دهید (مثلاً signup)
5. باید ببینید که درخواست‌ها به این آدرس می‌روند:
   ```
   https://mcl-backend.liara.run/auth/signup
   ```

---

## نکات مهم:

⚠️ **Cache مرورگر**: اگر همچنان localhost را می‌بینید:
- Cache مرورگر را پاک کنید (Ctrl + Shift + Delete)
- یا در حالت Incognito تست کنید

⚠️ **Service Worker**: اگر از Service Worker استفاده می‌کنید:
- در Developer Tools → Application → Service Workers
- روی "Unregister" کلیک کنید

⚠️ **متغیر محیطی**: برای محیط‌های مختلف:
- **Production**: URL مستقیماً در کد نوشته شده
- **Development**: می‌توانید فایل `.env` بسازید و متغیر را override کنید

---

## تست Local با URL لیارا:

اگر می‌خواهید قبل از دیپلوی تست کنید:

```bash
npm start
```

برنامه روی `http://localhost:3000` اجرا می‌شود ولی به `https://mcl-backend.liara.run` متصل خواهد شد.

⚠️ **توجه**: بک‌اند باید در لیارا در حال اجرا باشد و تنظیمات CORS آن به‌روز باشد.

---

## برگشت به حالت Development:

اگر می‌خواهید دوباره به localhost وصل شوید، فایل `.env` بسازید:

```
REACT_APP_API_BASE_URL=http://localhost:8081
```

این متغیر محیطی بر روی مقدار پیش‌فرض در کد تقدم دارد.

---

**موفق باشید! 🚀**

